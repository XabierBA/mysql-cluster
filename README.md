# MySQL Galera Cluster with HAProxy

Este proyecto configura un clúster de bases de datos **Galera** con **MySQL/MariaDB** y un **balanceador de carga HAProxy** utilizando Docker Compose.

## Requisitos

- Docker
- Docker Compose

## Pasos para ejecutar el proyecto

1. **Clonar el repositorio**:
   ```bash
   git clone https://github.com/tu-usuario/mysql-galera-cluster.git
   cd mysql-galera-cluster
